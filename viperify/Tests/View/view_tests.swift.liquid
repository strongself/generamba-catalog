//
//  {{ prefix }}{{ module_info.name }} - {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

@testable import {{ module_info.project_name | split: "-" | join: "_" }}
import Quick
import Nimble

class {{ module_info.name }}ViewTests: QuickSpec {
    override func spec() {
        describe("{{ module_info.name }} view") {
            var view: {{ module_info.name }}ViewController!
            var presenter: MockPresenter!
            beforeEach {
                presenter = MockPresenter()
                view = {{ module_info.name }}ViewController(viewOutput: presenter)
            }
        }
    }

    class MockPresenter: {{ module_info.name }}ViewOutput {
        var didCallViewIsReady = false
        func viewIsReady() {
            didCallViewIsReady = true
        }
    }
}
